import React from 'react';
import ReactDOM from 'react-dom';
import Example from '@gravityforms/components/react/example';

const init = ( target ) => {
	ReactDOM.render( <Example />, target );
};

export default init;
